# Local Docker Qdrant Configuration
# Copy this file to .env for local integration testing
#
# Start Qdrant with: docker compose -f docker-compose.test.yml up -d
# Run tests with: swift test --filter IntegrationTests

# Required: Qdrant REST API URL
QDRANT_URL=http://localhost:6333

# Optional: API key (not needed for local Docker)
QDRANT_API_KEY=

# Optional: Test collection name (defaults to "test-collection")
QDRANT_TEST_COLLECTION=test-collection
